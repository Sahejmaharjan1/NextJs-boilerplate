export enum ScreenSizes {
  MOBILE = 640,
  TABLET = 768,
  LARGE_TABLET = 1024,
  DESKTOP = 1280,
  LARGE_DESKTOP = 1440,
}

export enum NotifierTitle {
  GENERIC = 'generic',
}

export enum Orientation {
  LANDSCAPE = 'LANDSCAPE',
  PORTRAIT = 'PORTRAIT',
  SQUARE = 'SQUARE',
}
